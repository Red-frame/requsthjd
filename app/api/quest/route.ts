// app/api/quest/route.ts
import { NextResponse } from "next/server";

export async function GET() {
  // placeholder: return quest list later
  return NextResponse.json({ message: "Quest endpoint placeholder" });
}
